import { StikethroughDirective } from './stikethrough.directive';

describe('StikethroughDirective', () => {
  it('should create an instance', () => {
    const directive = new StikethroughDirective();
    expect(directive).toBeTruthy();
  });
});
